#!/usr/bin/env bash
# Wrapper for opencode that loads MCP secrets from pass
export IWIKI_MCP_URL="$(pass show mcp/iwiki-url)"
export IWIKI_MCP_TOKEN="$(pass show mcp/iwiki-token)"
exec /home/amos/.npm-packages/bin/opencode "$@"
