# -*- mode: snippet -*-
# name: is_sorted_until
# key: isu
# type: command
# --
(+amos/add-include "algorithm")
(yas-expand-snippet "auto pos = std::is_sorted_until(std::begin(${1:container}), std::end($1));")
if (pos != std::end($1)) {
    $2
}